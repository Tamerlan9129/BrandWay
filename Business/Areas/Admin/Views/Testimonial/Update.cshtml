@using Business.Areas.Admin.ViewModels.Testimonial;
@model TestimonialUpdateVM;

<div class="right_col" role="main">
    <div class="">
        ac
        <div class="row">
            <div class="col-md-12 col-sm-12">
                <div class="x_panel">
                    <div class="x_content">
                        <form enctype="multipart/form-data" asp-action="update" asp-controller="testimonial" asp-area="admin" method="post">
                            <div class="form-group">
                                <label asp-for="@Model.Title" class="col-form-label"></label>
                                <input asp-for="@Model.Title" class="form-control">
                                <span style="color:red !important" asp-validation-for="@Model.Title"></span>
                            </div>
                            <div class="form-group">
                                <label asp-for="@Model.Description" class="col-form-label"></label>
                                <textarea asp-for="@Model.Description" class="form-control"></textarea>
                                <span style="color:red !important" asp-validation-for="@Model.Description"></span>
                            </div>

                            <div class="custom-file mb-3">
                                <input asp-for="@Model.UserPhoto" class="custom-file-input" id="customFile">
                                <label class="custom-file-label" for="customFile" asp-for="@Model.UserPhoto"></label>
                                <span style="color:red !important" asp-validation-for="@Model.UserPhoto"></span>
                            </div>

                            <button class="btn btn-sm btn-outline-success" type="submit">Submit</button>
                            <a asp-action="index" asp-controller="testimonial" asp-area="admin" class="btn btn-sm btn-light text-dark">Cancel</a>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-select/1.13.1/css/bootstrap-select.css" />

@section Scripts{
    @await Html.PartialAsync("_FormValidationPartial")
    <script src="https://cdn.ckeditor.com/ckeditor5/35.2.1/classic/ckeditor.js"></script>
    <script>
        ClassicEditor
            .create(document.querySelector('#Description'))
            .then(editor => {
                console.log(editor);
            })
            .catch(error => {
                console.error(error);
            });
    </script>
        }