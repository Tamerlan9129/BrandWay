@using Business.Areas.Admin.ViewModels.Product;
@model ProductIndexVM;

<div class="right_col" role="main">
    <div class="">
        <div class="row">
            <a asp-action="create" asp-controller="product" class="btn btn-success">Create</a>
            <div class="col-md-12 col-sm-12 mt-4 ">
                <div class="x_panel">
                    <div class="x_content">
                        <div class="row">
                            <div class="col-sm-12">
                                <div class="card-box table-responsive">
                                    <table id="datatable" class="table table-striped table-bordered" style="width:100%">
                                        <thead>
                                            <tr>
                                                <th>Id</th>
                                                <th>Title</th>
                                                <th>Price</th>
                                                <th>Photo</th>
                                                <th>Brand</th>
                                                <th>Create date</th>
                                                <th>Update date</th>
                                                <th>Actions</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            @{
                                                foreach (var product in Model.Products)
                                                {
                                                    <tr>
                                                        <td>@product.Id</td>
                                                        <td>@product.Title</td>
                                                        <td>@product.Price</td>
                                                        <td><img style="width:150px;height:120px;object-fit:cover" class="img img-thumbnail" src="/assets/images/@product.MainPhoto" /></td>
                                                        <td>@product.Brand.Title</td>

                                                        <td>@product.CreatedAt</td>
                                                        <td>
                                                            @{
                                                                if (product.ModifiedAt.ToString() == "01.01.0001 00:00:00")
                                                                {
                                                                    <p>Non Modified</p>
                                                                }
                                                                else
                                                                {
                                                                    @product.ModifiedAt
                                                                }
                                                            }
                                                        </td>
                                                        <td style="width:240px">
                                                            <div class="btn-group ml-auto">
                                                                <form asp-action="delete" asp-controller="product" asp-area="admin" asp-route-id="@product.Id" style="display:flex;flex-wrap: wrap;gap: 5px;text-align:center;align-items:center;justify-content: center;">
                                                                    <div>
                                                                        <a asp-action="addsize" asp-controller="product" asp-route-id="@product.Id" class="btn btn-sm btn-light">Add Size</a>
                                                                        <a asp-action="addcolor" asp-controller="product" asp-route-id="@product.Id" class="btn btn-sm btn-light">Add Color</a>
                                                                    </div>
                                                                    <div>
                                                                        <a asp-action="details" asp-controller="product" asp-area="admin" asp-route-id="@product.Id" class="btn btn-sm btn-light">Info</a>
                                                                        <a asp-action="update" asp-controller="product" asp-area="admin" asp-route-id="@product.Id" class="btn btn-sm btn-light">Edit</a>
                                                                        <button type="submit" class="btn btn-sm btn-outline-danger ">
                                                                            <i class="far fa-trash-alt"></i>
                                                                        </button>
                                                                    </div>

                                                                </form>
                                                            </div>
                                                        </td>
                                                    </tr>
                                                }
                                            }
                                        </tbody>
                                    </table>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>